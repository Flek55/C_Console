#ifndef MYMUSICCONST_H
#define MYMUSICCONST_H

enum {
    FOUR = 4,
    LEN = 255
};

enum ProgramStatus {
    CORRECT,
    ERROR
};

enum Action {
    ADD = 1,
    LIST,
    DELETE,
    SORT,
    EXIT
};

#endif // MYMUSICCONST_H
